---
title: 碧蓝档案
icon: gamepad
order: 8
---

# 碧蓝档案

## 功能描述

碧蓝档案数据查询：总力战、好友码绑定、角色信息与伤害计算等。

本页功能由 **GsCore（早柚核心）** 的 `BlueArchiveUID` 插件提供，共 14 条指令。

::: tip 使用前提
1. 群聊需已开启 GsCore 相关功能（部分指令需 @机器人 或加前缀触发）。
2. 大部分查询需要先完成账号绑定，绑定方式见下方「账号绑定」分类。
3. 指令中标注 `需CK` 的表示需要先向 GsCore 提供对应平台的 Cookie/CK。
4. 发送 `帮助` 可让机器人在群内直接渲染帮助图。
:::

## 指令前缀

本插件支持的触发前缀：

| 前缀 | 说明 |
| ---- | ---- |
| `ba` | 该插件注册的触发前缀 |
| `BA` | 该插件注册的触发前缀 |

触发方式以「指令列表」中的 **示例** 列为准：示例里带了前缀的，实际发送时就需要带上。

## 指令列表

### 基础服务（3 条）

一些基础的查询功能

| 指令 | 说明 | 示例 |
| ---- | ---- | ---- |
| `攻略` | 获取主线任务走格子攻略 | `攻略16-3H` |
| `角色攻略` | 获取的角色的养成攻略 | `角色攻略优香` |
| `活动攻略` | 获取当期活动攻略 | `活动攻略` |

### 总力战相关（2 条）

数据来源arona.icu，需要申请Token使用

| 指令 | 说明 | 示例 |
| ---- | ---- | ---- |
| `总力战` | 获取当期国服总力战的档线信息 | `总力战` |
| `总力战b` | 获取当期B服总力战的档线信息 | `总力战b` |

### 绑定好友码（3 条）

在执行查询之前请绑定账号

| 指令 | 说明 | 示例 |
| ---- | ---- | ---- |
| `绑定好友码` | 绑定自己的账户 | `绑定好友码vlhy4mw:1` |
| `切换好友码` | 切换绑定的账户 · `需CK` | `切换好友码` |
| `删除好友码` | 解除绑定 | `删除好友码vlhy4mw:1` |

### 信息查询相关（2 条）

数据来源arona.icu，请在使用前进行【绑定】

| 指令 | 说明 | 示例 |
| ---- | ---- | ---- |
| `查询` | 获取当期国服总力战的档线信息 | `查询vlhy4mw:1` |
| `角色排名` | 获取全服的角色好感排名 | `角色排名星野` |

### 计算服务（3 条）

一些和计算相关的服务

| 指令 | 说明 | 示例 |
| ---- | ---- | ---- |
| `用时` | 根据分数计算用时 | `用时寿司1300w` |
| `算分` | 根据时间计算分数 | `算分ex寿司剩1:23.43` |
| `jjc挖矿` | 计算竞技场挖矿数量 | `jjc挖矿` |

### 其他服务（1 条）

一些杂项服务

| 指令 | 说明 | 示例 |
| ---- | ---- | ---- |
| `帮助` | 呼出该帮助界面 | `帮助` |

## 使用示例

（示例待补充）

::: details 通用游戏信息检索
除上述游戏内数据查询外，机器人还支持检索游戏基础信息（折扣、简介、评分等）。

| 指令 | 说明 | 示例 |
| ---- | ---- | ---- |
| `查询 <游戏名>` | 检索游戏名称，返回游戏的基础信息 | `查询 碧蓝档案` |
| `id <appid>` | 通过 AppID 反查游戏 | `id 570` |
| `打折游戏` | 查询当前打折的游戏 | `打折游戏` |
:::
