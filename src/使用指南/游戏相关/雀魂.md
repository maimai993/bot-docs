---
title: 雀魂
icon: gamepad
order: 6
---

# 雀魂

## 功能描述

雀魂麻将数据查询：牌谱、段位、对局数据与推送提醒等。

本页功能由 **GsCore（早柚核心）** 的 `MajsoulUID` 插件提供，共 18 条指令。

::: tip 使用前提
1. 群聊需已开启 GsCore 相关功能（部分指令需 @机器人 或加前缀触发）。
2. 大部分查询需要先完成账号绑定，绑定方式见下方「账号绑定」分类。
3. 指令中标注 `需CK` 的表示需要先向 GsCore 提供对应平台的 Cookie/CK。
4. 发送 `帮助` 可让机器人在群内直接渲染帮助图。
:::

## 指令前缀

本插件支持的触发前缀：

| 前缀 | 说明 |
| ---- | ---- |
| `雀魂` | 该插件注册的触发前缀 |
| `qh` | 该插件注册的触发前缀 |

触发方式以「指令列表」中的 **示例** 列为准：示例里带了前缀的，实际发送时就需要带上。

## 指令列表

### 基础服务（3 条）

需要先绑定uid, 注意好友码不是UID！！

| 指令 | 说明 | 示例 |
| ---- | ---- | ---- |
| `绑定uid` | 绑定你的雀魂UID | `绑定uid123456` |
| `删除uid` | 删除你的雀魂UID | `删除uid123456` |
| `切换uid` | 切换你的雀魂UID | `切换uid123456` |

### 查询服务（2 条）

查询一些基本信息, 需要在金之间拥有一定场次的对局

| 指令 | 说明 | 示例 |
| ---- | ---- | ---- |
| `搜索` | 搜索雀魂玩家 · `需CK` | `搜索Wuyi` |
| `查询` | 查询雀魂数据一览 · `需CK` | `查询1148850` |

### 推送提醒（2 条）

群内谁又偷偷上大分了？？

| 指令 | 说明 | 示例 |
| ---- | ---- | ---- |
| `订阅` | 在群内订阅机器人推送消息 · `需CK` | `订阅` |
| `好友排行榜` | 检查账号池内小号的好友排行榜 · `需CK` | `好友排行榜三麻` |

### 管理服务（10 条）

机器人管理员需要关注的服务

| 指令 | 说明 | 示例 |
| ---- | ---- | ---- |
| `添加账号` | 向账号池添加一个小号 · `需CK / 需管理员` | `登陆国服账号 密码` |
| `添加美服账号` | 向账号池添加一个小号 · `需CK / 需管理员` | `登陆美服账号 密码` |
| `添加日服账号` | 向账号池添加一个小号 · `需CK / 需管理员` | `登陆日服账号 密码` |
| `启动服务` | 每次重启机器人之后需要启动服务 · `需CK / 需管理员` | `启动服务` |
| `重启服务` | 遇到某些问题可能需要重启服务 · `需CK / 需管理员` | `重启服务` |
| `检查服务` | 遇到某些问题可能需要检查服务 · `需CK / 需管理员` | `检查服务` |
| `好友总览` | 检查账号池中小号的好友列表 · `需CK / 需管理员` | `好友总览` |
| `获取全部好友申请` | 检查账号池中小号的好友添加提醒 · `需CK / 需管理员` | `获取全部好友申请` |
| `同意所有好友申请` | 同意账号池中小号的好友添加提醒 · `需CK / 需管理员` | `同意所有好友申请` |
| `同意好友申请` | 同意账号池中小号的某个好友添加提醒 · `需CK / 需管理员` | `同意好友申请114514` |

### 其他服务（1 条）

一些杂项服务

| 指令 | 说明 | 示例 |
| ---- | ---- | ---- |
| `帮助` | 呼出该帮助界面 | `帮助` |

## 使用示例

（示例待补充）

::: details 通用游戏信息检索
除上述游戏内数据查询外，机器人还支持检索游戏基础信息（折扣、简介、评分等）。

| 指令 | 说明 | 示例 |
| ---- | ---- | ---- |
| `查询 <游戏名>` | 检索游戏名称，返回游戏的基础信息 | `查询 雀魂` |
| `id <appid>` | 通过 AppID 反查游戏 | `id 570` |
| `打折游戏` | 查询当前打折的游戏 | `打折游戏` |
:::
